```mermaid
flowchart TB
    subgraph Docker-Compose
        direction TB
        A[Frontend: Vue.js] -->|HTTP API| B[Backend: FastAPI]
        B -->|SQL| C[(Database: PostgreSQL)]
    end
    User[Пользователь] -->|HTTPS| A
    A -->|Static Files| Nginx[Nginx (Reverse Proxy)]
    Nginx --> B
    B --> C
    C --> B
    B --> A
```
